# Tickets 21, 22, 24 en 23 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Vier tickets afhandelen: de artikelverwijzing bij de categorietelling (#21), de te jonge
speler binnen dezelfde leeftijdscategorie (#22), de titel en omschrijving van de pagina (#24) en het
opruimen van het dode PDF-project (#23).

**Architecture:** Alle regellogica blijft in `rules.js`. #21 raakt `assessLevel`, #22 raakt
`assessAge`; die twee functies staan los van elkaar. #24 raakt `index.html` en `README.md` en krijgt
een nieuw testbestand dat die twee als tekst inleest. #23 verwijdert bestanden en de verwijzingen
ernaar. Elk ticket is een eigen taak met een eigen commit.

**Tech Stack:** ES-modules zonder build-stap, `node --test` als testrunner, `gh` voor de tickets.

## Global Constraints

- Code is Engels, alles wat een mens leest is Nederlands. Dat geldt voor variabelen, functies,
  commentaar, testnamen, DOM-id's en CSS-klassen (Engels) en voor paginateksten, artikelcitaten,
  `README.md`, `CLAUDE.md`, tickets en commitberichten (Nederlands). Zie `CLAUDE.md`.
- Geen em-dash, geen emoji, in geen enkele taal.
- Geen uitlijning van assignments over meerdere regels.
- Alle regellogica in `rules.js`, alle seizoensgegevens in `data.js`. `app.js` rekent niets uit.
- `articles.js` is gegenereerd en wordt nooit met de hand bewerkt.
- Elke wijziging gaat vergezeld van tests. Sneuvelt een bestaande test, pas hem dan aan en leg uit
  waarom de oude verwachting achterhaald was. Verwijder hem niet.
- Noem in tekst die de gebruiker ziet het artikelnummer dat het oordeel draagt.
- Bron voor elke regelbeslissing is `bronnen/bondsreglement-2026.pdf`, niet dit plan, niet de
  README, niet het commentaar dat er al staat.
- Testcommando: `npm test`. Een enkel testbestand: `node --test test/rules.test.js`.
- Branch: `tickets-21-22-24-23`. Werk daarop, niet op `main`.

---

### Task 1: Ticket #21, artikel 5.3.5.1 bij de categorietelling

**Files:**
- Modify: `rules.js` (de functie `assessLevel`, plus een nieuwe hulpfunctie erboven)
- Test: `test/rules.test.js` (nieuwe tests achteraan)

**Interfaces:**
- Consumes: `AGE_CATEGORY_ORDER` (bestaat al in `rules.js`), `assessLevel(lender, borrower)`.
- Produces: `assessLevel` geeft ongewijzigd `{ allowed, ground, conditions, reasoning, articles, caveats }`
  terug. Nieuw is alleen dat `articles` bij verschillende leeftijdscategorieen `"5.3.5.1"` bevat op
  elke grond behalve `fifth-class`, en dat `reasoning` er een regel bij heeft. Geen enkele
  `allowed`, `ground` of `conditions` verandert.

- [ ] **Step 1: Schrijf de falende tests**

Voeg achteraan `test/rules.test.js` toe:

```js
test("article 5.3.5.1 is named when the verdict is no across an age category boundary", () => {
  // Ticket #21: article 5.3.5.2 says "at most one class higher" but does not say how to count a
  // class across an age category boundary. That counting rule lives in article 5.3.5.1, so it
  // belongs in the list here too.
  const r = check("O14", "top", "O11", "1e");
  assert.equal(r.ground, "too-high");
  assert.ok(r.articles.includes("5.3.5.1"));
});

test("article 5.3.5.1 is named on ground one-higher across an age category boundary", () => {
  const r = check("O18", "2e", "O16", "2e");
  assert.equal(r.ground, "one-higher");
  assert.ok(r.articles.includes("5.3.5.1"));
});

test("the reasoning counts the age categories when the lender is younger", () => {
  // Article 5.3.5.1, second bullet: a team from a lower age category may play at most one class
  // higher, and per extra age category one class is added. O14 and O18 are two steps apart.
  const r = check("O14", "1e", "O18", "3e");
  const line = r.reasoning.find((v) => /leeftijdscategorie/.test(v));
  assert.ok(line, "no reasoning line about age categories");
  assert.ok(/2 leeftijdscategorieen/.test(line), line);
  assert.ok(/per extra leeftijdscategorie een klasse bij komt/.test(line), line);
});

test("one step apart does not claim an extra class", () => {
  const r = check("O12", "1e", "O14", "2e");
  const line = r.reasoning.find((v) => /leeftijdscategorie/.test(v));
  assert.ok(line, "no reasoning line about age categories");
  assert.ok(/1 leeftijdscategorie\b/.test(line), line);
  assert.ok(!/bij komt/.test(line), line);
});

test("the reasoning counts nothing when the lender is older", () => {
  // The third bullet of article 5.3.5.1 names no increase per age category. Whether it applies in
  // that direction too is the open question of ticket #12, so no number is written out here.
  const r = check("O18", "3e", "O16", "3e");
  const line = r.reasoning.find((v) => /Het uitlenende team speelt in een hogere/.test(v));
  assert.ok(line, "no reasoning line about the older lender");
  assert.ok(!/leeftijdscategorieen\./.test(line), line);
  assert.ok(!/bij komt/.test(line), line);
});

test("ground fifth-class keeps its articles unchanged", () => {
  // Ticket #3 deliberately removed 5.3.5.1 here, and this ground only applies within the same age
  // category, so there is no boundary to cross.
  const r = check("O14", "5e", "O14", "6e");
  assert.equal(r.ground, "fifth-class");
  assert.ok(!r.articles.includes("5.3.5.1"));
});

test("within the same age category no reasoning line about age categories appears", () => {
  const r = check("O14", "2e", "O14", "3e");
  assert.ok(!r.reasoning.some((v) => /leeftijdscategorie/.test(v)));
});

test("every article appears at most once in the list of assessLevel", () => {
  for (const category of AGE_CATEGORIES) {
    for (const classId of CLASSES[category].map((k) => k.id)) {
      const r = check(category, classId, "O14", "3e");
      assert.equal(new Set(r.articles).size, r.articles.length, `${category} ${classId}`);
    }
  }
});
```

- [ ] **Step 2: Draai de tests en stel vast dat ze falen om de juiste reden**

Run: `node --test test/rules.test.js`
Expected: FAIL. De eerste twee op `r.articles.includes("5.3.5.1")` die false is, de derde tot en
met de vijfde op een ontbrekende redeneringsregel (`no reasoning line about ...`). De laatste drie
slagen al, want die leggen bestaand gedrag vast.

- [ ] **Step 3: Voeg de hulpfunctie toe aan `rules.js`**

Zet dit direct boven `export function assessLevel(lender, borrower) {`:

```js
// Article 5.3.5.1 defines what counts as an equal or lower playing team across an age category
// boundary, and that definition is what decides the verdict as soon as lender and borrower sit in
// different age categories. So the article and its counting rule belong in the explanation on
// every ground, also where the verdict is no (ticket #21). Returns null within the same age
// category, which is why ground fifth-class never gets this line: that ground applies within one
// age category by definition.
function ageCategoryReasoning(lender, borrower) {
  const lenderIndex = AGE_CATEGORY_ORDER.indexOf(lender.category);
  const borrowerIndex = AGE_CATEGORY_ORDER.indexOf(borrower.category);
  const steps = Math.abs(lenderIndex - borrowerIndex);
  if (steps === 0) return null;
  if (lenderIndex > borrowerIndex) {
    // The third bullet names no increase per age category. Whether it applies in this direction
    // too is the open question of ticket #12, so no number is written out here.
    return `Het uitlenende team speelt in een hogere leeftijdscategorie. Artikel 5.3.5.1 staat dat toe als dat team minimaal een klasse lager speelt dan het team waarin wordt ingevallen.`;
  }
  const opening = `${lender.category} en ${borrower.category} schelen ${steps} leeftijdscategorie${steps === 1 ? "" : "en"}.`;
  const rule = "Artikel 5.3.5.1 zegt dat een team uit een lagere leeftijdscategorie maximaal een klasse hoger mag spelen";
  if (steps === 1) return `${opening} ${rule}.`;
  return `${opening} ${rule}, en dat er per extra leeftijdscategorie een klasse bij komt.`;
}
```

- [ ] **Step 4: Laat artikelen maar een keer in de lijst komen**

`assessLevel` verzamelt artikelen op vier plekken en zou 5.3.5.1 anders dubbel opnemen bij grond
`equal-or-lower`. Voeg direct onder `const caveats = [];` in `assessLevel` toe:

```js
  // Articles are collected in several places and may overlap, for example 5.3.5.1 from the age
  // category explanation below and from ground equal-or-lower. The list is what the page renders,
  // so every number appears in it at most once.
  function addArticles(...numbers) {
    for (const number of numbers) {
      if (!articles.includes(number)) articles.push(number);
    }
  }
```

Vervang daarna in `assessLevel` elke `articles.push(...)` door `addArticles(...)`. Dat zijn deze
regels, ongewijzigd op de naam na:

```js
    articles.push("3.1.1", "3.1.3");                              ->  addArticles("3.1.1", "3.1.3");
    articles.push("5.3.5.4");                                     ->  addArticles("5.3.5.4");
    articles.push("5.3.5.3");                                     ->  addArticles("5.3.5.3");
    articles.push("5.3.4", "5.3.6", "5.3.6.1", "5.1.1");          ->  addArticles("5.3.4", "5.3.6", "5.3.6.1", "5.1.1");
    articles.push("5.3.5.1");                                     ->  addArticles("5.3.5.1");
    articles.push("3.1.3");                                       ->  addArticles("3.1.3");
    articles.push("5.3.5.2");                                     ->  addArticles("5.3.5.2");
```

`articles.push("5.3.4", "5.3.6", "5.3.6.1", "5.1.1")` staat drie keer in de functie en
`articles.push("3.1.3")` twee keer; vervang ze allemaal.

- [ ] **Step 5: Roep de hulpfunctie aan**

Zoek in `assessLevel` het blok dat de niveauregel in `reasoning` zet. Dat eindigt met:

```js
  } else {
    reasoning.push(`${describe(lender)} speelt volgens de tabel klassengrenzen ${borrowerLevel - lenderLevel} niveau${borrowerLevel - lenderLevel === 1 ? "" : "s"} hoger dan ${describe(borrower)}.`);
  }
```

Zet daar direct onder:

```js
  // Explains, in the terms of article 5.3.5.1, how a class is counted across an age category
  // boundary. The line comes right after the level line from the class boundaries table, so the
  // article stands next to the table instead of replacing it. Within the same age category this
  // yields null and nothing changes, which also covers ground fifth-class.
  const ageCategoryLine = ageCategoryReasoning(lender, borrower);
  if (ageCategoryLine) {
    reasoning.push(ageCategoryLine);
    addArticles("5.3.5.1");
  }
```

- [ ] **Step 6: Draai de tests**

Run: `npm test`
Expected: PASS, alles groen. Faalt een bestaande test, dan is er een uitkomst veranderd die niet
had mogen veranderen; dit ticket verandert alleen tekst en artikelnummers.

- [ ] **Step 7: Controleer met eigen ogen dat de tekst klopt**

Run:

```bash
node -e 'import("./rules.js").then(m=>{
  for (const [a,b,c,d] of [["O14","1e","O18","3e"],["O14","top","O11","1e"],["O18","2e","O16","2e"],["O14","5e","O14","6e"]]) {
    const r = m.assessLevel({category:a,classId:b},{category:c,classId:d});
    console.log(`${a} ${b} -> ${c} ${d} [${r.ground}] ${r.articles.join(", ")}`);
    for (const line of r.reasoning) console.log("  " + line);
  }
})'
```

Expected: bij de eerste drie staat 5.3.5.1 in de artikelen en staat er een regel over
leeftijdscategorieen bij; bij `O14 5e -> O14 6e` staat geen van beide.

- [ ] **Step 8: Commit**

```bash
git add rules.js test/rules.test.js
git commit -m "fix: noem artikel 5.3.5.1 waar de telling per leeftijdscategorie het antwoord draagt

Artikel 5.3.5.2 zegt maximaal een klasse hoger, maar hoe je over een
categoriegrens telt wat een klasse is staat in 5.3.5.1. Dat artikel stond
alleen bij de grond equal-or-lower en ontbrak bij one-higher en too-high.
De redenering legt nu ook uit hoeveel leeftijdscategorieen er schelen.

Bij een oudere uitlener wordt geen aantal genoemd: de derde bullet van
5.3.5.1 kent daar geen ophoging, en of die er wel is staat open in #12.

Deel van #21"
```

---

### Task 2: Ticket #22, te jong binnen dezelfde leeftijdscategorie

**Files:**
- Modify: `rules.js` (de functie `assessAge`)
- Test: `test/rules.test.js` (nieuwe tests achteraan)

**Interfaces:**
- Consumes: `assessAge(lender, borrower, dateOfBirth)` en `assess(lender, borrower, dateOfBirth)`,
  beide bestaand.
- Produces: `assessAge` geeft ongewijzigd `{ age, blocks, messages, articles, conditions }` terug.
  Nieuw: `blocks` is false wanneer de speler onder de ondergrens zit en bron en doel in dezelfde
  leeftijdscategorie zitten, met een uitleg in `messages`.

- [ ] **Step 1: Schrijf de falende tests**

Voeg achteraan `test/rules.test.js` toe:

```js
test("a player already in the borrower category is not blocked on the lower age limit", () => {
  // Ticket #22: an eleven year old can only be on an O14 team list when the competition
  // management placed her there (article 3.1.3), so that assessment has already been made. The
  // tool must not make it again and come to a different answer.
  const outcome = assess(
    { category: "O14", classId: "5e" },
    { category: "O14", classId: "5e" },
    d("2015-08-20"),
  );
  assert.equal(outcome.verdict, "allowed");
});

test("the same player is allowed from a younger category too, and the answers match", () => {
  const fromO12 = assess({ category: "O12", classId: "5e" }, { category: "O14", classId: "5e" }, d("2015-08-20"));
  const fromO14 = assess({ category: "O14", classId: "5e" }, { category: "O14", classId: "5e" }, d("2015-08-20"));
  assert.equal(fromO12.verdict, "allowed");
  assert.equal(fromO14.verdict, fromO12.verdict);
});

test("the age explanation points at the dispensation of article 3.1.3", () => {
  const outcome = assessAge(
    { category: "O14", classId: "5e" },
    { category: "O14", classId: "5e" },
    d("2015-08-20"),
  );
  assert.equal(outcome.blocks, false);
  const line = outcome.messages.find((v) => /3\.1\.3/.test(v));
  assert.ok(line, "no message about article 3.1.3");
  assert.ok(/ondergrens/.test(line), line);
  assert.ok(outcome.articles.includes("3.1.3"));
});

test("an older lender with a player under the lower limit keeps blocking", () => {
  // Whether a dispensation for O16 also makes her eligible in O14 is nowhere in the regulations.
  // CLAUDE.md then prescribes the conservative side, so this stays no until the KNHB answers.
  const outcome = assess(
    { category: "O16", classId: "5e" },
    { category: "O14", classId: "5e" },
    d("2015-08-20"),
  );
  assert.equal(outcome.verdict, "not-allowed");
});

test("too old within the same age category keeps blocking", () => {
  // Article 5.2.4 does say in so many words that those players may only appear for their own team.
  // For too young no comparable provision exists, which is why the two differ.
  const outcome = assessAge(
    { category: "O12", classId: "1e" },
    { category: "O12", classId: "1e" },
    d("2013-08-20"),
  );
  assert.equal(outcome.blocks, true);
});

test("a player within the limits gets no dispensation note", () => {
  const outcome = assessAge(
    { category: "O14", classId: "5e" },
    { category: "O14", classId: "5e" },
    d("2013-08-20"),
  );
  assert.equal(outcome.blocks, false);
  assert.ok(!outcome.messages.some((v) => /3\.1\.3/.test(v)));
});
```

- [ ] **Step 2: Draai de tests en stel vast dat ze falen om de juiste reden**

Run: `node --test test/rules.test.js`
Expected: FAIL. De eerste drie falen omdat `verdict` nu `not-allowed` is en `blocks` true; de
laatste drie slagen al, want die leggen bestaand gedrag vast dat overeind moet blijven.

Let op de tweede test: die faalt op `assert.equal(fromO14.verdict, fromO12.verdict)` en laat
precies het probleem uit het ticket zien, dezelfde speler met twee verschillende antwoorden.

- [ ] **Step 3: Pas `assessAge` aan**

Zoek in `assessAge` deze twee takken:

```js
  } else if (age < borrowerLimits.min && !lenderFromYoungerCategory) {
    blocks = true;
    messages.push(`Op ${dateText} is de speler ${age} jaar en daarmee te jong voor ${borrower.category}, waar de ondergrens ${borrowerLimits.min} jaar is. Dit mag alleen met dispensatie van de competitieleiding.`);
    articles.push("3.1.1", "3.1.3");
  } else if (age < borrowerLimits.min) {
    messages.push(`Op ${dateText} is de speler ${age} jaar. Dat is jonger dan de ondergrens van ${borrowerLimits.min} jaar voor ${borrower.category}, maar de speler komt uit een jongere leeftijdscategorie, dus dat is hier de normale situatie en geen afwijking.`);
  } else {
```

Vervang die twee takken door een tak die de drie gevallen uit elkaar houdt:

```js
  } else if (age < borrowerLimits.min) {
    if (lenderFromYoungerCategory) {
      messages.push(`Op ${dateText} is de speler ${age} jaar. Dat is jonger dan de ondergrens van ${borrowerLimits.min} jaar voor ${borrower.category}, maar de speler komt uit een jongere leeftijdscategorie, dus dat is hier de normale situatie en geen afwijking.`);
    } else if (lender.category === borrower.category) {
      // Ticket #22: the player is on a team list in exactly the category she substitutes in. She
      // can only be there because the competition management placed her there, and article 3.1.3
      // says that body decides in advance in which age category a player is placed. So that
      // question has already been answered and the tool must not answer it again. What remains is
      // a note: check that the dispensation is really there.
      messages.push(`Op ${dateText} is de speler ${age} jaar, jonger dan de ondergrens van ${borrowerLimits.min} jaar voor ${borrower.category}. Zij staat al op een teamlijst in ${borrower.category}, dus de competitieleiding heeft die indeling al beoordeeld (artikel 3.1.3). Ga na dat die dispensatie er is.`);
      articles.push("3.1.1", "3.1.3");
    } else {
      // The lender comes from an older age category. Whether a dispensation for that older
      // category also makes her eligible here is nowhere in the regulations, so this keeps
      // blocking: the conservative side, see the ticket about those cases.
      blocks = true;
      messages.push(`Op ${dateText} is de speler ${age} jaar en daarmee te jong voor ${borrower.category}, waar de ondergrens ${borrowerLimits.min} jaar is. Dit mag alleen met dispensatie van de competitieleiding.`);
      articles.push("3.1.1", "3.1.3");
    }
  } else {
```

Het commentaarblok boven `const lenderFromYoungerCategory = ...` beschrijft nu alleen de jongere
bron. Vul de laatste zin ervan aan zodat hij klopt met de drie takken:

```js
  // A lender from a younger age category is by definition often too young for the borrower
  // category: that is precisely the normal situation when borrowing from a younger category
  // (article 5.3.5.1), not a deviation. So this must not block. Too old for the borrower category
  // and the exception of article 5.2.4 still block, also with a younger lender. And "too young"
  // no longer blocks when lender and borrower sit in the same age category either: the player is
  // then on a team list in exactly that category (ticket #22). Only with an older lender does it
  // still block.
```

- [ ] **Step 4: Draai de tests**

Run: `npm test`
Expected: PASS, alles groen.

- [ ] **Step 5: Tel na hoeveel gevallen er veranderd zijn**

Run:

```bash
node -e 'import("./rules.js").then(async m=>{
  const {AGE_CATEGORIES, CLASSES} = await import("./data.js");
  const order = ["O11","O12","O14","O16","O18"];
  let same = 0, older = 0, younger = 0;
  for (const lc of AGE_CATEGORIES) for (const lk of CLASSES[lc])
   for (const bc of AGE_CATEGORIES) for (const bk of CLASSES[bc])
    for (let year = 2007; year <= 2017; year++) {
      const lender = {category:lc, classId:lk.id};
      const borrower = {category:bc, classId:bk.id};
      const r = m.assess(lender, borrower, new Date(Date.UTC(year,7,20)));
      // Only the cases where the class allows it and the age is what blocks. A too-high class
      // blocks on its own and says nothing about the age rule.
      if (r.verdict === "out-of-scope") continue;
      if (!m.assessLevel(lender, borrower).allowed) continue;
      if (!r.age || !r.age.blocks) continue;
      if (r.age.age >= 0 && r.age.messages.some((v) => /te jong|ondergrens/.test(v))) {
        if (lc === bc) same++; else if (order.indexOf(lc) > order.indexOf(bc)) older++; else younger++;
      }
    }
  console.log({same, older, younger});
})'
```

Expected: `{ same: 0, older: 2192, younger: 0 }`. De 1616 uit het ticket zijn weg, de 2192 blijven
staan zoals afgesproken. Wijkt `older` af van 2192, meld dat dan in plaats van het cijfer aan te
passen; dan klopt er iets anders niet.

- [ ] **Step 6: Commit**

```bash
git add rules.js test/rules.test.js
git commit -m "fix: blokkeer geen speler die al op een teamlijst in de doelcategorie staat

Een elfjarige in een O14-team mocht niet invallen bij een ander O14-team,
terwijl dezelfde speler dat vanuit O12 wel mocht. Zij kan daar alleen staan
omdat de competitieleiding haar heeft ingedeeld, en artikel 3.1.3 zegt dat
die dat vooraf beoordeelt. In plaats van een nee staat er nu een
kanttekening in het leeftijdsblok die om die dispensatie vraagt.

Een uitlener uit een oudere leeftijdscategorie blijft blokkeren; daar zegt
het reglement niets over en dat krijgt een eigen ticket.

Deel van #22"
```

- [ ] **Step 7: Maak het vervolgticket aan**

```bash
gh issue create --repo faijdherbe/invalmatrix --label bug --title "Speler onder de ondergrens wordt geweigerd wanneer de uitlener uit een oudere leeftijdscategorie komt (2192 gevallen)" --body "$(cat <<'EOF'
Afgesplitst van #22. Dat ticket loste de 1616 gevallen op waarin bron en doel in dezelfde
leeftijdscategorie zitten. Deze 2192 gevallen bleven bewust staan, omdat het antwoord er minder
duidelijk is.

## Het geval

Een speler van elf jaar staat op de teamlijst van een O16-team en wil invallen in O14. De tool
zegt nee, omdat elf jaar onder de ondergrens van twaalf jaar voor O14 ligt.

Reproductie: bron O16 5e klasse, doel O14 5e klasse, geboortedatum 20 augustus 2015.

## De vraag

Zij zit alleen in O16 omdat de competitieleiding haar daar heeft ingedeeld. Artikel 3.1.3 zegt dat
die vooraf beoordeelt in welke leeftijdscategorie een speler wordt ingedeeld. Maakt die
beoordeling haar ook speelgerechtigd in O14, een categorie waar zij op leeftijd evenmin in past?

Het reglement zegt daar niets over. Artikel 5.3.5.1, derde bullet, zegt alleen dat invallers uit
een hogere leeftijdscategorie moeten voldoen aan de leeftijdsgrenzen van de categorie waarin ze
invallen. Dat pleit voor blokkeren. Maar diezelfde eis zou dan ook gelden voor de speler die al in
de doelcategorie staat, en daar is bij #22 juist besloten dat de eerdere beoordeling voorgaat.

## Wat de tool nu doet

Blokkeren, de behoudende kant, zoals CLAUDE.md voorschrijft bij een reglement dat iets in het
midden laat. Zie de derde tak in \`assessAge\` in \`rules.js\`.

## Wat nodig is

Een antwoord van de KNHB-competitieleiding. Dit is geen keuze die wij zelf kunnen maken.

## Verwant

#22 (de opgeloste helft), #15 (dezelfde spanning tussen artikel 3.1.3 en artikel 5.3.5.1).
EOF
)"
```

Noteer het nummer dat `gh` teruggeeft; dat komt in de afsluitende toelichting van #22.

---

### Task 3: Ticket #24, titel, kop en omschrijving

**Files:**
- Modify: `index.html:6` (de `<title>`), `index.html:10` (de `<h1>`), en een nieuwe `<meta>` in de
  `<head>`
- Modify: `README.md:1` (de eerste kop)
- Create: `test/page-text.test.js`

**Interfaces:**
- Consumes: niets uit de eerdere taken.
- Produces: niets waar latere taken op leunen. `test/page-text.test.js` leest `index.html` en
  `README.md` als platte tekst; het importeert geen enkele module van het project.

- [ ] **Step 1: Schrijf het falende testbestand**

Maak `test/page-text.test.js`:

```js
import { test } from "node:test";
import assert from "node:assert/strict";
import { readFileSync } from "node:fs";

// Reads the page and the README as plain text. Ticket #24: a tab, a bookmark, a search result and
// a shared link must say what this is about. That means naming the sport and the association, and
// it means not opening with the association, because the page is not a product of the KNHB and the
// footer says so in so many words.
const html = readFileSync(new URL("../index.html", import.meta.url), "utf8");
const readme = readFileSync(new URL("../README.md", import.meta.url), "utf8");

function tagContent(source, tag) {
  const match = source.match(new RegExp(`<${tag}[^>]*>([^<]*)</${tag}>`));
  return match ? match[1].trim() : null;
}

test("the title names both field hockey and the KNHB", () => {
  const title = tagContent(html, "title");
  assert.ok(title, "no title tag found");
  assert.match(title, /veldhockey/i);
  assert.match(title, /KNHB/);
});

test("the title does not open with the KNHB, so it does not read as their product", () => {
  const title = tagContent(html, "title");
  assert.ok(!/^KNHB/.test(title), title);
  assert.ok(!/officiele/i.test(title), title);
});

test("the heading names both field hockey and the KNHB", () => {
  const heading = tagContent(html, "h1");
  assert.ok(heading, "no h1 found");
  assert.match(heading, /veldhockey/i);
  assert.match(heading, /KNHB/);
});

test("there is a meta description that names the KNHB", () => {
  const match = html.match(/<meta\s+name="description"\s+content="([^"]*)"/);
  assert.ok(match, "no meta description found");
  assert.match(match[1], /KNHB/);
  assert.ok(match[1].length > 60, "the description is too short to be useful in a link preview");
});

test("the first heading of the README names both field hockey and the KNHB", () => {
  const heading = readme.split("\n").find((line) => line.startsWith("# "));
  assert.ok(heading, "no first level heading found in README.md");
  assert.match(heading, /veldhockey/i);
  assert.match(heading, /KNHB/);
});
```

- [ ] **Step 2: Draai de test en stel vast dat hij faalt om de juiste reden**

Run: `node --test test/page-text.test.js`
Expected: FAIL. Vier van de vijf falen: de titel is `Invalmatrix` en noemt geen veldhockey, de h1
net zo, er is geen meta description, en de README-kop is `# Invalmatrix`. De test die controleert
dat de titel niet met KNHB begint slaagt al.

- [ ] **Step 3: Pas `index.html` aan**

Vervang in de `<head>`:

```html
<title>Invalmatrix</title>
```

door:

```html
<title>Invalmatrix voor de veldhockeycompetities van de KNHB</title>
<meta name="description" content="Mag een jeugdspeler uit het ene team invallen in het andere? Deze pagina beantwoordt dat per leeftijdscategorie en klasse, op grond van het KNHB Bondsreglement 2026 en de tabel klassengrenzen. Geen officieel product van de KNHB.">
```

Vervang in de `<header>`:

```html
  <h1>Invalmatrix</h1>
```

door:

```html
  <h1>Invalmatrix voor de veldhockeycompetities van de KNHB</h1>
```

De contextregel eronder (`<p class="context" id="context">`) blijft ongewijzigd. Die wordt door
`app.js` uit `data.js` gevuld en verandert per seizoen mee.

- [ ] **Step 4: Pas `README.md` aan**

Vervang de eerste regel:

```markdown
# Invalmatrix
```

door:

```markdown
# Invalmatrix voor de veldhockeycompetities van de KNHB
```

De tweede alinea eronder blijft ongewijzigd.

- [ ] **Step 5: Draai de tests**

Run: `npm test`
Expected: PASS, alles groen, inclusief de bestaande testbestanden.

- [ ] **Step 6: Bekijk de pagina**

Run: `python3 -m http.server 8000` en open http://localhost:8000. Controleer dat de tabtitel de
volledige zin toont en dat de kop op de pagina niet over twee regels breekt op een manier die
lelijk staat op een smal scherm. Breekt hij lelijk, meld dat dan in plaats van de tekst zelf te
verzinnen; de formulering ligt vast in het ontwerp.

- [ ] **Step 7: Commit**

```bash
git add index.html README.md test/page-text.test.js
git commit -m "docs: maak in titel, kop en README duidelijk dat dit over de KNHB-veldhockeycompetities gaat

De pagina heette overal alleen Invalmatrix. Dat zegt niets in een tab, een
bladwijzer, een zoekresultaat of een gedeelde link. Er staat nu bij om welke
sport en welke competities het gaat, en er is een meta description voor het
linkvoorbeeld.

De formulering zegt waar het over gaat, niet van wie het is: een titel als
KNHB Invalmatrix zou de disclaimer in de voettekst tegenspreken.

Deel van #24"
```

- [ ] **Step 8: Zet de omschrijving en de topics op GitHub**

```bash
gh repo edit faijdherbe/invalmatrix \
  --description "Mag een jeugdspeler invallen in een ander team? Antwoord per leeftijdscategorie en klasse, op grond van het KNHB Bondsreglement. Geen officieel product van de KNHB." \
  --add-topic hockey --add-topic veldhockey --add-topic knhb --add-topic jeugdhockey --add-topic competitie
```

Controleer daarna:

```bash
gh repo view faijdherbe/invalmatrix --json description,repositoryTopics
```

Expected: de omschrijving staat er en er staan vijf topics.

- [ ] **Step 9: Sluit het ticket**

```bash
gh issue close 24 --repo faijdherbe/invalmatrix --comment "Opgelost. De titel, de h1 en de eerste kop van de README zijn nu 'Invalmatrix voor de veldhockeycompetities van de KNHB', en er is een meta description bijgekomen voor het linkvoorbeeld. De repo heeft een omschrijving en de topics hockey, veldhockey, knhb, jeugdhockey en competitie.

De formulering zegt waar het over gaat en niet van wie het is, zoals het ticket vroeg. 'KNHB' staat niet vooraan, zodat het niet leest als een product van de bond en de disclaimer in de voettekst niet wordt tegengesproken.

Vastgelegd in test/page-text.test.js: die leest index.html en README.md als tekst en controleert dat titel, kop, meta en README-kop zowel veldhockey als KNHB noemen, en dat de titel niet met KNHB begint. De test faalde eerst op vier van de vijf punten en slaagt nu."
```

---

### Task 4: Ticket #23, restanten van het eerdere PDF-project opruimen

**Files:**
- Delete: `build.py`, `invalmatrix-meisjes-2026-2027.pdf`, `HANDOVER.md`,
  `docs/superpowers/specs/2026-08-30-invalmatrix-checker-design.md`,
  `docs/superpowers/plans/2026-08-30-invalmatrix-checker.md`,
  `docs/superpowers/plans/2026-08-30-reglement-audit-fixes.md`
- Modify: `README.md` (de sectie "Verwant" en de bestandenlijst), `CLAUDE.md` (regel 16 en 28, plus
  een regel over de toon), `test/rules.test.js:242` (de testnaam)

**Interfaces:**
- Consumes: niets uit de eerdere taken.
- Produces: niets. Alleen het ontwerp en dit plan blijven in `docs/superpowers/` staan.

- [ ] **Step 1: Pas de testnaam aan en stel vast dat hij nog draait**

De twaalf gevallen blijven, alleen de naam en de toelichting worden losgetrokken van een bestand
dat straks niet meer bestaat. Vervang in `test/rules.test.js`:

```js
test("the twelve cases from build.py give the same outcome", () => {
```

door:

```js
// Twelve combinations from articles 5.3.5.1 up to and including 5.3.5.3 and the class boundaries
// table, kept together as regression cover: they touch every ground once and cross the age
// category boundary in both directions.
test("twelve combinations from the articles and the class boundaries table keep their outcome", () => {
```

Run: `node --test test/rules.test.js`
Expected: PASS. De test draait onder zijn nieuwe naam en de twaalf gevallen zijn ongewijzigd.

- [ ] **Step 2: Verhuis de werkafspraak uit HANDOVER.md naar CLAUDE.md**

In `HANDOVER.md` staat onder "Stijlvoorkeuren van de opdrachtgever" een regel die niet in
`CLAUDE.md` staat. Voeg in `CLAUDE.md` onderaan de sectie `## Taal` toe, direct onder de regel
`Verder in alle talen: geen em-dash, geen emoji.`:

```markdown
De toon van het Nederlands is casual en direct.
```

De andere regel daar, "volledige bestanden regenereren in plaats van inline patches", vervalt
bewust: er wordt nu met gerichte bewerkingen gewerkt. De openstaande vragen uit `HANDOVER.md` staan
al als ticket in de issue tracker, zoals dat bestand zelf zegt.

- [ ] **Step 3: Haal HANDOVER.md uit CLAUDE.md**

Regel 16, in de opsomming van wat Nederlands is. Vervang:

```markdown
artikelcitaten uit het Bondsreglement, `README.md`, `HANDOVER.md`, dit bestand, de tickets op
```

door:

```markdown
artikelcitaten uit het Bondsreglement, `README.md`, dit bestand, de tickets op
```

Regel 28, in de regel over wat geen bron is. Vervang:

```markdown
Niet `HANDOVER.md`, niet `README.md`, en niet het commentaar dat al in de code staat. Die kunnen
```

door:

```markdown
Niet `README.md`, en niet het commentaar dat al in de code staat. Die kunnen
```

- [ ] **Step 4: Haal de sectie "Verwant" uit de README**

Verwijder deze hele sectie onderaan `README.md`:

```markdown
## Verwant

`build.py` en `invalmatrix-meisjes-2026-2027.pdf` komen uit een eerder project dat een losse
PDF-matrix genereerde voor de meisjesjeugd. Ze staan los van deze pagina en worden er niet door
gebruikt. Zie `HANDOVER.md` voor de achtergrond van dat project en de openstaande vragen voor de
competitieleiding.
```

Controleer daarna de bestandenlijst in `README.md` op andere verwijzingen:

```bash
grep -n "build.py\|HANDOVER\|meisjes" README.md
```

Expected: geen enkele regel. Komt er wel iets terug, haal het dan ook weg.

- [ ] **Step 5: Verwijder de bestanden**

```bash
git rm build.py invalmatrix-meisjes-2026-2027.pdf HANDOVER.md \
  docs/superpowers/specs/2026-08-30-invalmatrix-checker-design.md \
  docs/superpowers/plans/2026-08-30-invalmatrix-checker.md \
  docs/superpowers/plans/2026-08-30-reglement-audit-fixes.md
```

`git rm`, niet gewoon van de schijf gooien: alles blijft in de geschiedenis staan en is terug te
halen met `git show <commit>:<pad>`.

- [ ] **Step 6: Controleer dat er niets meer naar verwijst**

```bash
grep -rn "build\.py\|HANDOVER\|invalmatrix-meisjes" . --exclude-dir=.git
```

Expected: alleen treffers in `docs/superpowers/specs/2026-08-30-tickets-21-22-24-23-design.md` en
`docs/superpowers/plans/2026-08-30-tickets-21-22-24-23.md`, want die twee beschrijven dit ticket
zelf. Komt er een ander bestand terug, ruim dat dan ook op.

- [ ] **Step 7: Draai de tests en laad de pagina**

Run: `npm test`
Expected: PASS, alles groen. Er hing niets aan de verwijderde bestanden, maar dat moet bevestigd
worden in plaats van aangenomen.

Run: `python3 -m http.server 8000` en open http://localhost:8000. Controleer dat het raster
verschijnt en dat de console geen fout toont over een ontbrekend bestand.

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "chore: ruim de restanten van het eerdere PDF-project op

build.py, de gegenereerde PDF en HANDOVER.md komen uit een eerder project
dat een losse matrix-PDF maakte. build.py bevatte een tweede implementatie
van dezelfde invalregels die niemand onderhield en die zou gaan afwijken
van rules.js. De plannen en het ontwerp uit het bouwproces gaan mee: die
verwijzen naar bestanden en functienamen die niet meer bestaan, en de
git-geschiedenis is het archief.

De twaalf regressiegevallen blijven; alleen de testnaam is verankerd in de
artikelen in plaats van in build.py. De toonafspraak uit HANDOVER.md staat
nu in CLAUDE.md.

Deel van #23"
```

- [ ] **Step 9: Sluit het ticket**

```bash
gh issue close 23 --repo faijdherbe/invalmatrix --comment "Opgeruimd. Weg met git rm: build.py, invalmatrix-meisjes-2026-2027.pdf, HANDOVER.md en de drie documenten in docs/superpowers/ uit het bouwproces. Alles blijft opvraagbaar met git show <commit>:<pad>.

Over de map docs/superpowers/ die het ticket openliet: weggegooid, want de git-geschiedenis is het archief. De map zelf blijft bestaan met het ontwerp en het plan van deze ronde erin, die wel actueel zijn.

Meegenomen: de sectie 'Verwant' is uit README.md, CLAUDE.md noemt HANDOVER.md niet meer op regel 16 en 28, en de test met de twaalf gevallen heet nu naar de artikelen waar die gevallen vandaan komen in plaats van naar build.py. De gevallen zelf zijn ongewijzigd.

Een werkafspraak uit HANDOVER.md die nergens anders stond ('Nederlands, casual en direct') is naar CLAUDE.md verhuisd. De regel over volledige bestanden regenereren is bewust vervallen.

Vastgesteld met npm test (alles groen) en met de pagina lokaal geladen: het raster verschijnt en de console meldt geen ontbrekend bestand."
```

---

### Task 5: Review over de hele branch

**Files:**
- Modify: geen, tenzij de review iets oplevert
- Test: `npm test`

**Interfaces:**
- Consumes: het resultaat van taak 1 tot en met 4.
- Produces: een groene branch, klaar om te mergen.

- [ ] **Step 1: Draai de volledige testsuite**

Run: `npm test`
Expected: PASS. Noteer het aantal tests; dat aantal gaat in de README (zie stap 3).

- [ ] **Step 2: Bekijk de hele diff**

Run: `git diff main...HEAD`

Loop hem na op: Nederlands in alles wat de gebruiker leest, Engels in de code, geen em-dash, geen
emoji, geen uitgelijnde assignments, en geen regellogica die buiten `rules.js` is beland.

- [ ] **Step 3: Werk het testaantal in de README bij**

`README.md` noemt op twee plekken een aantal tests ("Er zijn 134 tests" en "`test/`: de 134
tests"). Dat aantal klopt na deze branch niet meer. Vervang beide door het aantal uit stap 1, en
noem `test/page-text.test.js` in de opsomming van waar de tests over gaan.

```bash
grep -n "134" README.md
```

Expected na de wijziging: geen treffers.

- [ ] **Step 4: Sluit ticket #21**

```bash
gh issue close 21 --repo faijdherbe/invalmatrix --comment "Opgelost. Artikel 5.3.5.1 staat nu ook in de artikelenlijst bij de gronden one-higher en too-high zodra bron en doel in verschillende leeftijdscategorieen zitten, en de redenering legt de categoriegrens uit in de termen van het artikel.

Bij een jongere uitlener wordt de telling uitgeschreven: 'O14 en O18 schelen 2 leeftijdscategorieen. Artikel 5.3.5.1 zegt dat een team uit een lagere leeftijdscategorie maximaal een klasse hoger mag spelen, en dat er per extra leeftijdscategorie een klasse bij komt.'

Bij een oudere uitlener wordt geen aantal genoemd, alleen de derde bullet geciteerd. Die bullet kent namelijk geen ophoging per leeftijdscategorie, en of die er toch is staat als open vraag in #12. Een getal noemen dat er niet staat zou dat ticket stilzwijgend beantwoorden.

De grond fifth-class is niet aangeraakt, zoals het ticket vroeg: daar is 5.3.5.1 bij #3 bewust uit de lijst gehaald en daar spelen categoriegrenzen niet.

Vastgelegd in test/rules.test.js met acht tests, waaronder een die controleert dat de uitkomsten zelf niet veranderen: dit ticket verandert alleen wat er te lezen staat, niet wat er mag."
```

- [ ] **Step 5: Sluit ticket #22**

Vul `<nummer>` in met het nummer van het ticket uit taak 2, stap 7.

```bash
gh issue close 22 --repo faijdherbe/invalmatrix --comment "Opgelost voor de 1616 gevallen waarin bron en doel in dezelfde leeftijdscategorie zitten. Die blokkade is vervallen: de speler staat daar al op een teamlijst, dus de competitieleiding heeft die indeling al beoordeeld (artikel 3.1.3) en de tool hoort die vraag niet per wedstrijd opnieuw te stellen.

In plaats van een nee staat er nu een kanttekening in het leeftijdsblok: 'Op 1 oktober 2026 is de speler 11 jaar, jonger dan de ondergrens van 12 jaar voor O14. Zij staat al op een teamlijst in O14, dus de competitieleiding heeft die indeling al beoordeeld (artikel 3.1.3). Ga na dat die dispensatie er is.' Het oordeel is Ja. Geen voorwaarde, want het raster kent geen geboortedatum en zou dan een eis tonen die het niet kan onderbouwen.

Het geval uit het ticket (bron O14 5e, doel O14 5e, geboortedatum 20 augustus 2015) geeft nu hetzelfde antwoord als hetzelfde geval vanuit O12: Ja.

Niet opgelost, en bewust: de 2192 gevallen waarin de uitlener uit een oudere leeftijdscategorie komt. Daar zegt het reglement niets over, en CLAUDE.md schrijft dan de behoudende kant voor. Dat staat nu als #<nummer>.

Vastgesteld met zes tests in test/rules.test.js en met een telling over alle combinaties en elf geboortejaren: 0 blokkades binnen dezelfde categorie, 2192 met een oudere uitlener, precies zoals afgesproken."
```

- [ ] **Step 6: Commit en rond af**

```bash
git add README.md
git commit -m "docs: werk het aantal tests in de README bij"
npm test
git log --oneline main..HEAD
```

Expected: alles groen, en vijf tot zes commits op de branch.

---

## Wat er open blijft staan

- Ticket #12: kent de derde bullet van artikel 5.3.5.1 dezelfde ophoging per leeftijdscategorie?
  Zolang dat openstaat noemt de redenering bij een oudere uitlener geen aantal.
- Ticket #11: tellen O11 en O12 als een niveau of als twee leeftijdscategorieen? Dat bepaalt het
  getal dat de nieuwe redenering noemt.
- Het nieuwe ticket uit taak 2 over de 2192 gevallen met een oudere uitlener.
